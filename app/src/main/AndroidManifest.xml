<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

          package="com.sandamil.buscaminas"


          android:versionCode="1"
          android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application android:icon="@drawable/mine2" android:label="@string/app_name" android:debuggable="true">
        <activity android:name=".activities.StartActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.MenuActivity"></activity>
        <activity android:name=".activities.PlayActivity"></activity>
        <activity android:name=".activities.OptionsActivity"></activity>
        <activity android:name=".activities.GameActivity"></activity>

    </application>

</manifest>